#define _CRT_SECURE_NO_WARNINGS
#include <cstdio>
#include "graph.h"

void graphColorsGreedy(int** graph, int numberOfVertices) {
	for (int vortexID = 0; vortexID < numberOfVertices; vortexID++) {
		

	}
}