#define _CRT_SECURE_NO_WARNINGS
#include <cstdio>
#include "graph.h"

void printColorsSLF(int* colors, int numberOfVertices) {
    /*for (int i = 0; i < numberOfVertices; i++) {
		printf("%d ", colors[i]);
	}
	printf("\n");*/
}

void graphColorsSLF(int** graph, int numberOfVertices, int* sortedDegrees, int* sortedVertexMap) {
    printf("?\n");
	/*int* numberOfColoredNeighbours = new int[numberOfVertices];
    int* colors = new int[numberOfVertices];
    int* usedColors = new int[numberOfVertices];
    int maxColorUsed = 0;
	for (int vertexID = 0; vertexID < numberOfVertices; vertexID++) {
		numberOfColoredNeighbours[vertexID] = 0;
	}
    for (int i = 0; i < numberOfVertices; i++) {
        colors[i] = 0;
    }
    for (int vertexID = 0; vertexID < numberOfVertices; vertexID++) {
        for (int i = 0; i <= maxColorUsed; i++) {
            usedColors[i] = 0;
        }

    }
    printColorsSLF(colors, numberOfVertices);
    delete[] colors;
    delete[] usedColors;*/
}